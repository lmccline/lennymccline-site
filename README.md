# lennymccline.com

Personal site. Astro, plain CSS, deployed to Cloudflare Workers static assets.

- `src/pages/` — home page
- `src/content/essays/` — one markdown file per essay
- `npm run dev` — local
- `npm run deploy` — build and ship
